<h1>Listing to_do_categories</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @to_do_categories.each do |to_do_category| %>
  <tr>
    <td><%= to_do_category.name %></td>
    <td><%= link_to 'Show', to_do_category %></td>
    <td><%= link_to 'Edit', edit_to_do_category_path(to_do_category) %></td>
    <td><%= link_to 'Destroy', to_do_category, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New To do category', new_to_do_category_path %>
